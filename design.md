# ZServer 设计思路

## 地图瓦片与 Actor 模型的天然契合

我需要设计一个地图的 WEB 服务，支持 OGC 的 WMS 和 WMTS 两种接口。
对于 WMTS 其瓦片的唯一性天然和 Actor 模型契合， 因此可以很方便的管理每一个瓦片的更新、渲染， 而不用担心并发锁的问题
对于 WMS 也可以使用多个 Actor (根据负载、集群数据) 来做水平的扩容

请帮我整理一下设计思路。